Desktop preview QA: updated navigation rendered with Case studies and Notes; header utilities rendered; bot demo, deep-dive tabs, GitHub widget, notes, feedback, availability, skill evidence and resume link were present in DOM. GitHub widget loaded public repo cards after async fetch. Initial visual layout preserved the dark editorial direction. Next checks: theme/language toggle, bot actions, deep-dive data switching, modal focus, mobile layout, console errors.
Theme QA: clicking theme toggle changed the icon/state from ☼/dark to ☾/light and rendered the light palette across the hero and navigation. Layout remained structurally intact, although the dark theme remains the recommended brand default. Next check: restore dark theme, test Bengali toggle and interaction controls.
Interaction QA: Bengali toggle successfully localized primary navigation and key section/hero headings while preserving the English subcontent; language control changed to EN. Bot Search PDFs action was reachable and appended the simulated interaction flow. Light mode remained active during this check, and long project sections rendered without runtime interruption. Next checks: deep-dive tab data, modal preview, mobile viewport, console errors.
Deep-dive QA: DeepSeek tab changed the selected case-study content and retained the six-field Problem/Thinking/Technical decision/Challenge/Solution/Outcome grid. The visual case-study control remained available after switching, and the existing modal opened with the MRI artwork/links while the page stayed in place. Light theme and Bengali navigation persisted as expected.
Responsive/runtime QA: 390px mobile screenshot showed the updated MR header mark, utility toggles, compact navigation and headline wrapping cleanly; hero CTA/status remained readable. Browser console returned no output/errors during the tested session. Desktop/mobile visual checks are clean enough for final packaging; external GitHub data remains graceful-fallback capable.

Feature expansion runtime check: The updated local preview loaded successfully at port 4181. The header now exposes About, Services, Work, Case studies, Notes, Build Notes, Process and Contact, plus Email, GitHub and Telegram quick links. The hero rotation renders the current capability phrase, and the page exposes the new terminal form, newsletter form and owner-only analytics route markup. The LinkedIn shortcut remains visibly marked as pending because no user LinkedIn URL was provided. Browser extraction showed no load-time HTML failure.

The current screenshot confirms the existing portrait and editorial dark navy/gold composition remain intact while the new feature launcher is available in the lower-right corner. Further interaction checks cover terminal commands, assistant retrieval, analytics query mode and form fallback behavior.

Interaction QA: The terminal form accepted `projects` through the keyboard submit path and the page remained stable. The project assistant launcher opened and a local knowledge-base question returned the verified services answer; the note correctly stated that no question was sent to an external AI service. This confirms the static fallback path works without any endpoint or API key.

Analytics QA: Opening `index.html?analytics=1` exposed the owner console and displayed local browser-mode counters, including recorded visits and assistant questions. The console explicitly states that local mode is not site-wide analytics and that a protected endpoint is required for aggregate traffic data. No runtime error occurred during this route test.

Final mobile visual QA: At 390px width, the MR header mark, theme/language controls, rotating hero headline, availability card, project CTA and AI assistant launcher remained readable and within the viewport. The dark editorial palette and portrait treatment were preserved. The assistant launcher intentionally sits above the mobile sticky CTA and does not cover the hero copy.

Case-study image correction QA: local preview loaded successfully. The visual case-study modal opened with `project-mri-vault-relatable.webp`; browser reported naturalWidth 2560, confirming that the artwork path now resolves. Root-level fallback resolution is also present for alternate packaged asset paths.
Light-theme QA: representative section titles/body copy/project text/modal text computed to dark readable colors (`#171925` / `#424b5c`) on the cream palette. One sampled `.form-field input` still showed a dark input background, so selector context is being checked before final packaging.
Final light-theme contrast QA: after the high-specificity guard loaded, section titles and body copy remained readable (`rgb(23,25,37)` / `rgb(66,75,92)`), while contact form `#name` and `#message` resolved to white backgrounds with dark text. Modal text also remained readable on the cream dialog.
Full-page light-theme visual QA: hero/about, build-notes newsletter and contact form rendered cleanly through the page; contact controls visibly use white fields with dark labels/text, and the fixed back-to-top control appeared after scrolling. The cream/blue editorial palette remains readable while preserving the dark-navy/gold brand treatment in interactive terminal/analytics surfaces.
Runtime interaction QA: changing the project comparison dropdown to DeepSeek updated the facts to the conversational-AI stack and in-progress proof path. Submitting the LLM/prototype fit quiz returned a contextual DeepSeek recommendation. The back-to-top control became visible after scrolling, and `?project=deepseek` deep-link opened the corresponding DeepSeek artwork/panel route successfully.
Public preview QA: exposed URL loaded the corrected dark editorial build with all primary navigation, project cards, artwork references, tools and footer links present. Browser console inspection returned no output/errors. Final ZIP integrity test passed with no extraction errors.
New feature runtime QA: The refreshed service-worker v5 served the new script correctly. Actual button clicks produced a Fit Quiz 2.0 recommendation (`LLM Integrations service · Focused build · Within 1 month`) and a structured brief with export buttons enabled. Client portal preview opened, its Milestones tab switched content, and the compact section TOC opened with `aria-expanded=true`. The initial synthetic submit check was intentionally replaced by real button-click QA because browser validation correctly governs submit events.
Final localization/accessibility QA: after v6 activation, English ↔ Bangla switching changed nav and Fit Quiz option labels (`Prototype exists` ↔ `Prototype আছে`). Fit Quiz returned an LLM growth-build recommendation; brief export buttons enabled; comparison changed to Personal Portfolio Website; portal Handover tab rendered; TOC `aria-expanded=true`. Assistant focus moved to `ai-input` and returned to `ai-launcher`; modal focus moved to `dialog-close` and returned to `deep-preview`. Final console inspection contained no uncaught errors.
Technical-proof visual QA: in light mode, the embedded MRI Vault workflow screen and trade-off/failure-handling card rendered cleanly with readable dark editorial text, pale card surfaces and the existing fixed TOC/back-to-top utilities. The section retains the safe-demo notice and no external Telegram session is loaded.
Client portal visual QA: in light mode, the static-safe portal preview rendered as a strong dark workspace panel with DEMO ONLY badge, Overview/Milestones/Handover tabs and readable handover metrics. The explanatory copy clearly warns that protected backend authentication and private storage are required before confidential data is used.
Final public conversion-preview QA: the exposed URL loaded the latest dark editorial build with Fit Quiz 2.0 fields, brief-generator actions, availability calendar content and new conversion controls present in the DOM. Browser console returned no output/errors. Static source checks passed with 194 unique IDs, no missing local references and meaningful alt text on every image.
Bengali localization QA checkpoint: service-worker v6 cache initially served the previous script, so the cache was reset during local QA and sw.js was bumped to v7. Fresh source contains the new full localization dictionary; a second browser pass is checking the language toggle and dynamic sections after cache reset.
Bengali v9 visual QA: fresh cache reset loaded the supplementary dictionary and translated navigation, hero copy, about lead, services copy, project descriptions, bot walkthrough, buttons, placeholders, Fit Quiz labels, brief labels and proof copy. Remaining English in the sampled DOM is primarily technical names/stacks plus a few dynamic GitHub/analytics status strings; those are being handled with explicit dynamic branches or intentionally preserved identifiers.
Bengali v10 visual QA: full-page dark/cream render remains stable; navigation, hero, about, services, case-study cards, bot demo controls, comparison controls, Fit Quiz controls, brief generator labels/placeholders, proof copy and GitHub View actions now render in Bengali where applicable. The remaining untranslated strings are being reduced to technical identifiers, code/stack names and a few long dynamic section fields.
Bengali v11 QA: final fresh reload shows Bengali navigation, headings, hero/about copy, services, project descriptions, case-study controls, terminal prompt, comparison actions, Fit Quiz options, brief fields/placeholders, GitHub actions and contact pathways. English retained only for proper names, code/stack identifiers, repository names, URLs and technical terms where translation would reduce clarity; all ordinary explanatory prose is translated.
Final Bengali interaction QA: English regression returned `lang=en`, English navigation, English services title and English brief placeholder. Bengali interaction pass returned `lang=bn`, an LLM Integration recommendation with Bengali deadline, a Bengali structured brief, Bengali local assistant response, Bengali terminal skills response, and Bengali Milestones portal content. Console inspection showed no uncaught runtime errors.
Public preview final QA: after resetting the public origin's stale service-worker cache, the exposed preview served the latest Bengali-complete build. Bengali toggle changed the live public shell to Bengali across navigation, hero, project controls, form labels/placeholders, Fit Quiz controls, bot demo actions and assistant/portal-ready sections. English fallback remains available through the same toggle.
Attachment v12 QA checkpoint: new Notes search/tag controls and Bengali labels are present after cache reset; outcome and booking markup are in the refreshed local build. One final check is validating the new data-en-placeholder/data-bn-placeholder sync and interactions after the service-worker refresh.
Attachment v13 QA: after service-worker reset, Bengali Notes search input now shows `topic, project বা keyword দিয়ে খুঁজুন…`; note labels and the rest of the Bengali shell remain stable. Next runtime pass will exercise search, tag filtering, outcome section visibility and the unconfigured discovery-call fallback.
Attachment feature runtime QA: Bengali Notes search for `Termux` returned one matching note; clearing the query and selecting the LLM tag returned one LLM note. Outcome Evidence rendered four proof cards. Unconfigured discovery-call CTA produced the Bengali fallback message and routed toward the brief generator without inventing a calendar URL.
Attachment final QA: `node --check script.js` and `node --check sw.js` passed. HTML validator reported 198 IDs, 7 images, 6 forms, 12 required feature IDs; duplicate IDs, missing local images, empty image alt text and missing hooks were not found. Service-worker version v13 matched. Final ZIP extraction integrity passed with 21 entries; SHA-256: 6aaeb3463c8ccb7fdd7f3509ae3b9621d7681a61b0a4745880e8a84031e6f976.
Public v13 preview QA: after resetting the public origin's stale service-worker cache, Bengali mode showed the new Notes search placeholder (`topic, project বা keyword দিয়ে খুঁজুন…`), Bengali note controls and the feature-complete shell. Public preview remains available at the exposed 4183 URL.
Targeted v14 local QA checkpoint: new Simplified View control, six stack nodes, three snippet-copy controls, three pricing cards, four project-timeline items, meta-case-study and Currently Building widget are present. The initial stack readout test still showed the default text, indicating the local origin was serving a stale pre-v14 script; cache reset is required before judging the handler.
Targeted v14 runtime QA after cache reset: six interactive stack nodes are present and focusing Python updates the readout to Bengali tool/project usage text. Three snippet copy controls, three pricing cards, four project-timeline items and the meta-case-study are present. Simplified View toggles `data-view="simple"`, changes the button to `পূর্ণ`, and restores correctly. Currently Building fetched a public GitHub event and showed `Personal-portfolio-website-এ public update হয়েছে` in Bengali mode.
Targeted v14 final static QA: script.js and sw.js syntax passed; validator reported 217 IDs, 6 active image references and 6 forms; no duplicate IDs, missing local images, empty image alt text or missing core hooks. Fifteen targeted feature IDs were found for stack network, Currently Building, snippets, packages, project timeline, meta case study and Simplified View. Service-worker cache is v14. Browser console review after the fresh cache reset showed no uncaught runtime error; the successful stack readout, snippet controls, simplified state and GitHub status checks are recorded above.
Public v14 QA: after clearing the public origin's stale cache, the Bengali shell showed the Simplified View control as `সহজ`, the localized Notes placeholder, snippet controls and the current-building GitHub status. The public preview is serving the latest targeted build.
Initial v15 local QA checkpoint: the new markup was present and Ask About Me plus four skills-proof tags were visible, but the first runtime probe still served an older service-worker script, so command items and custom SVG heatmap were absent. Reading-progress text also remained in the old English form. A v15 cache reset is required before judging the latest handlers.
Fresh v15 local QA after clearing the service-worker cache: Cmd+K opens a palette with 11 commands; Ask About Me is localized as `আমাকে জিজ্ঞেস করুন`; custom GitHub heatmap renders as an SVG with 126 cells; reading progress is localized as `পড়ার অগ্রগতি3%`; status endpoint hook is present but intentionally empty; four skills carry verified-project proof tags. Print control is present and schedules `window.print()` asynchronously.
Final v15 runtime QA after normal reload: Cmd/Ctrl+K opened 11 command items and Escape closed the palette; custom GitHub SVG heatmap rendered with 126 cells; Bengali reading progress, Ask About Me label, four verified skill tags and print-resume control were present. Status endpoint remained intentionally empty, so the status surface stayed in its honest unconfigured state. The native print preview itself was not automated further because invoking `window.print()` opens a browser-owned print dialog; the control and print-mode CSS were validated without altering the normal preview state.
047 v16 browser QA checkpoint: Fresh local preview at http://127.0.0.1:4183/ loaded successfully. Existing page shell, new navigation state, project controls, notes, comparison, Fit Quiz and brief form rendered. Language toggle switched from persisted Bengali to English; lang=en, English nav and English form placeholders appeared. The preview screenshot showed the dark/cream editorial layout, local portrait and responsive header. New interactive sections still require targeted scroll/click checks below.
048 v16 targeted browser QA: Initial browser origin served a stale pre-v16 DOM, so a query-string cache-buster was used. Fresh `?v16qa=1` and `?v16qa=2` loads exposed recruiter mode, demo sandbox, workflow reel, code playground, accessibility audit and all expected controls. Programmatic interaction verified safe retrieve output, non-executing Python structural preview, workflow frame rendering and share metadata title behavior. The first audit heuristic reported 4/5 because button text was not counted as a label; the heuristic was corrected to count visible control text and to inspect reduced-motion CSS evidence. Static syntax and validator checks passed after the correction.
049 v17 final browser QA: After the v17 service-worker bump and a second reload to activate the new worker, the corrected handler was served. The local accessibility surface returned 5/5 checks passed with the explicit non-certification caveat. The demo backup action returned a local SQLite snapshot/recovery message with no external request; the Python playground reported 4 non-empty lines, one function, Path/database hints and no execution/system access. Recruiter mode persisted through localStorage and exposed its concise summary panel. Fresh page controls and visual case-study image loading remained present.
050 v17 localization and conversion QA: Bengali toggle changed the new demo heading to `builder-কে hire করার আগে workflow দেখুন।`, the workflow reel button to `workflow preview চালান`, and the playground action to `নিরাপদ output preview করুন →`; switching back restored English and `lang=en`. Filling and submitting the structured brief enabled the Print/PDF action and rendered `QA visitor · project brief`. Light-theme sampled section-title text remained `rgb(23,25,37)` on the light palette. QA localStorage preferences were then cleared so the final preview returns to default English/dark/full/recruiter-off state.
051 Final preview QA: After clearing preferences, `?finalqa=1` loaded with default English, dark theme, full view and recruiter-off. New demo-lab, code-playground and recruiter controls remained in the DOM, the service-worker script was v17, and the final console view showed only expected QA expressions/results with no uncaught exceptions. The final screenshot preserved the navy/gold editorial layout, local portrait, navigation and status cards.
052 v17 packaging QA: Clean deployable archive `/home/ubuntu/Rifat-portfolio-v17.zip` contains 48 entries, excludes prior ZIP artifacts, and passed `unzip -tq`. SHA-256: `156a4b3d990c53c49e1748c846422457738a12b7b2e46bb0babb31f300996dcd`. Public preview remains available at https://4183-ih3eq9kgv77c3syrirv4m-dcd118f9.sg1.manus.computer/.
053 v18 initial browser QA: Fresh `?v18qa=1` rendered the new proof ledger, decision replay, scenario builder, readiness, architecture actions, changelog, API, feedback, profile export, keyboard, PWA and reading-mode controls. Bengali/English title localization switched correctly. The first synthetic DOM interaction batch reported that proof/decision/scenario/readiness/architecture/keyboard handlers did not update as expected; this is a targeted QA issue under investigation before packaging.
054 v18 direct-click QA: After a second cache-refresh reload, proof filter hid 2 of 4 cards for the configuration-hook view and restored all cards; DeepSeek decision replay changed to the trade-off content; scenario builder changed to the automation scenario in English and Bengali; readiness meter reached 100%; architecture SVG export reported success; changelog diff rendered `v15 · proof layer → v17 · accessibility pass`; keyboard guide opened; reading mode toggled on and off. No browser exception was observed in this pass.
055 v18 utility QA: Fresh v18 page successfully produced a feedback preview with pending permission, then an approved-for-review state without publishing or sending; JSON profile export reported download success; PWA action honestly reported that the native install prompt was available in the browser. API copy uses an asynchronous clipboard path and needs a delayed status assertion; the first changelog localization check was run while the page was in English, so it will be repeated with an explicit Bengali state.
056 v19 cache/localization QA: The first v19 reloads still served an older service-worker script, so the local registration and both v18/v19 caches were explicitly cleared. After a clean reload, Bengali mode rendered the proof-ledger heading and changelog diff bullets correctly: `Verified claim ও configuration hook-সহ proof ledger`, `Scenario builder, readiness signal ও architecture export`, and `Engineering principles, API boundary note ও portable profile export`. This confirms the latest script and v19 cache state are active.
057 v19 accessibility/theme QA: The local audit returned `5/5 local checks passed. This is not certification.` Light theme rendered the proof-ledger title in `rgb(23, 25, 37)` over body background `rgb(243, 239, 230)`, confirming readable dark text on the light canvas. Final preference reset returned reading mode to `false` and simplified view to `full`.
058 v19 final browser console: Latest console output contained only expected QA expressions/results; no uncaught exception was displayed. The final recorded assertions remain: Bengali changelog localization passed after a clean cache reset, local accessibility audit 5/5, light-theme proof title readable, reading mode false, simplified view full.
059 v19 packaging QA: `/home/ubuntu/Rifat-portfolio-v19.zip` contains 48 entries, excludes previous ZIP artifacts, passed `unzip -tq`, and has SHA-256 `39efce85e41e5eeb5f8100ef4ab13bb0b9dc0dd16dd9d7812d74beab35dc52e2`. Service-worker cache is `rifat-portfolio-v19`.
060 v20 initial QA: New performance, privacy, anti-fit, outcome-planner and contact QR/vCard IDs rendered; QR image completed at 192px and Bengali static headings translated. The first direct-click batch did not update performance scan, outcome plan, privacy status or QR toggle, so runtime handler activation is under targeted diagnosis before release.
061 v20 diagnosis: Browser fetch confirmed the served `script.js` contains `runPerformanceScan`, `outcomePlans`, vCard handler and language refresh hook. The current document still showed unchanged handler outputs, indicating a stale service-worker client rather than missing code; a clean local cache/client reset is the next QA step.
062 v20 clean-client runtime QA: After unregistering the service worker and clearing the v20 cache, direct clicks passed. Performance scan reported 8 first-party resources and about 169ms DOMContentLoaded in the local preview; outcome planner rendered `Measure the repeated step`; QR panel opened with `Hide contact QR`; QR image was loaded at 192px; vCard download reported success; privacy reset reported local preferences and analytics events cleared.
063 v20 localization/accessibility QA: Bengali mode translated the performance-budget title, privacy title, anti-fit title, outcome-plan heading and QR control; the local scan status also translated. English state was restored. Accessibility audit returned `5/5 local checks passed. This is not certification.` Light theme rendered the new title with `rgb(23, 25, 37)` over `rgb(243, 239, 230)`.
064 v20 final browser regression: Final preview required IDs were present; performance scan reported 8 first-party resources and about 184ms DOMContentLoaded in the local preview; outcome planner rendered `Make the assistant understandable`; QR panel opened and the 192px local QR image was loaded; privacy remained unchanged after the QR toggle. No runtime exception appeared in the direct assertion.
065 v20 packaging QA: `/home/ubuntu/Rifat-portfolio-v20.zip` contains 49 deployable entries including `contact-qr.png`, excludes performance-analysis helper artifacts, passed `unzip -tq`, and has SHA-256 `8bdf05013d250c958f08e55d7bfd07290389ced91854306c3aa7ea76a1ef541e`. Service-worker cache is `rifat-portfolio-v20`; final preview remains available at the exposed port 4183 URL.
070 v21 mobile visual QA: 390x844 headless screenshot initially exposed an empty desktop-tools grid row that pushed the hamburger down; mobile CSS now hides the empty desktop tools container, keeps the brand and hamburger in one row, constrains the menu height for viewport scrolling, places utility controls inside the open menu, locks body scroll while open, and gives the hamburger spans an explicit width. Final screenshot shows a clean single-row header with visible three-line menu icon and stable hero separation.
071 v21 contrast diagnosis: The 390px CDP client confirmed mobile menu open/close state, body locking and utility relocation. Browser-loaded stylesheet includes and matches the v21 light selectors, but the existing browser client still reported old token-derived nav/contact colors after a theme toggle; this requires a new service-worker cache version/fresh client before final contrast verification. Static section-title/body-copy colors already computed as dark readable values.
072 v21 contrast guard: Explicit `!important` light overrides were added for nav links, contact links, utility toggles and footer links. Static validation still passes. Fresh CDP mobile behavior remains pass (menu top row, 5 utility controls inside open menu, body lock). The active browser session predates the final guard; a clean v21 browser client will be used for the final computed-style assertion.
073 v21 transition diagnosis: The fresh client loaded the final v21 `!important` selectors and matched them. Immediate computed-style reads still showed the previous dark token because navigation/link controls retain a 250ms color transition during theme switching; delayed post-transition sampling is required before judging contrast. Inline style test confirmed the same transient behavior.
074 v21 contact contrast diagnosis: The contact link matches the explicit v21 `!important` selector and the stylesheet reports the rule, while nav/title/body/utility computed colors are readable. The link retains the legacy `.contact-links a` color transition; the final fix will remove transition-based theme lag for light-mode contact/navigation surfaces and verify again after a forced style flush.
075 v21 final visual QA: Fresh browser light-mode preview shows readable hero title, body copy, navigation, utility controls, project content and contact surfaces. The v21 mobile CDP assertion passes with 5 utility buttons inside the open menu, menu top at 65px within the viewport, body scroll locked while open, correct ARIA state, and clean close reset. Final computed light colors: nav `rgb(53,64,82)`, contact `rgb(72,82,99)`, utility `rgb(53,64,82)`, title `rgb(23,25,37)`, body `rgb(66,75,92)`.
076 v21 packaging QA: Clean `/home/ubuntu/Rifat-portfolio-v21.zip` contains 26 deployable source/assets/docs entries, excludes old archives, performance helper files, mobile screenshots and CDP helper, passed `unzip -tq`, size 14,601,194 bytes, SHA-256 `1a2e74b701c23423b93e9578e893337f6636a3b75a5ace8c01d5b97a600931cd`.
077 v22 runtime QA: Fresh client loaded the five new surfaces. Security layer selection changed the output, failure simulator rendered the model-unavailable recovery state, deployment inspector returned 7/7 local checks pass, high-contrast and large-text preferences set and reset their root data states, and Bengali toggle rendered the new dynamic failure/security text before restoring English.
078 v22 final fresh-client QA: After clearing the v22 service-worker cache, security layer 03, empty-result failure simulation, and deployment inspector were verified. Deployment returned 7/7 local checks pass. Bengali mode rendered the security title, failure title and security fact labels, then restored English; accessibility preference reset returned root states to motion=full, contrast=normal, text=normal and focus=false. One technical label `Review signal` remains intentionally concise but will be polished to `পর্যালোচনার signal` in Bengali.
079 v22 localization polish: Fresh client rendered Security Explorer labels as `এখানে visible`, `পরে protected`, `পর্যালোচনার signal`, rendered the Bengali dynamic title `Secret static bundle-এর বাইরে থাকে।`, and restored English after the toggle test.
080 v22 final packaging: Clean `/home/ubuntu/Rifat-portfolio-v22.zip` contains 26 deployable source/assets/docs entries, passes `unzip -tq`, size 14,611,114 bytes, SHA-256 `0151b0fbc93eff0edfb72f46b730ea18dd81f7aca7944747bb3b0a8f8220e067`.
081 v23 readability correction: Added a late-cascade light-theme readability layer with explicit slate/gold contrast tokens, stronger muted colors, readable backgrounds, larger body/supporting copy, improved line-height, larger form and interactive labels, Bengali tracking correction, and expanded large-text preference coverage. Fresh English light mode at 1280px computed body/project copy at 16px, skill proof at 14.4px, project metadata/security controls at 12.48–13.44px; fresh dark mode retained the editorial palette while raising high-value proof labels. Bengali light mode and dynamic sections were checked. A fresh 390x844 CDP viewport passed mobile menu open/close, body scroll lock, ARIA state, five utility controls in the menu, and readable mobile light typography. Service-worker cache is v23; stylesheet cache-busting is style.css?v=23.5. Static validator reports ids=398, images=7, forms=12, required_features=80, all checks OK.

082 v23.5 final packaging: Clean `/home/ubuntu/Rifat-portfolio-v23.5.zip` contains 27 deployable source/assets/docs entries, passes `unzip -tq`, size 14,616,192 bytes, SHA-256 `97db7044daf47d8901b0ab5bb0be9bcbb0333b91e85f154ca8245d627af2b85f`. The archive contains the v23 readability CSS, v23 service-worker, cache-busted `style.css?v=23.5`, validator, documentation and light-mode audit.
083 v24 feature expansion QA: Added Integration Settings with local-only public endpoint hooks for contact, safe demo, analytics, status and newsletter; Project Proof Lab with read-only local fallback; Repository Detail cards; Guided Portfolio Tour; dedicated bilingual `resume.html`; browser-native brief Print/Save PDF surface; shareable comparison panel; editable `content-releases.json` feed; Status Integration fallback; expanded Section TOC. Fresh v24.1 client found all 9 new sections, stylesheet/script cache-busted references, 3 release-feed items and deployment readiness 7/7 local checks pass. Proof Lab switched to DeepSeek; repository output switched to DeepSeek; guided tour opened; Bengali toggle translated the new integration title and proof copy and restored English. Integration settings saved public example hooks and reset them; status fallback stayed honest; empty brief-PDF state routed users back to the Brief Generator without triggering external requests. Dedicated resume route loaded and bilingual toggle rendered Bengali recruiter copy. Browser console had no output/errors.
084 v24 mobile QA: Fresh 390x844 CDP viewport passed existing mobile header behavior with menu open/close, body scroll lock, ARIA state and five utility controls in the menu. Mobile light typography remains readable: nav 15.04px, hero/body 16.32px, form labels 13.44px, security controls 14.4px and accessibility descriptions 12.8px. Static validation reports ids=453, images=7, forms=13, required_features=114; HTML structure, images, alt text, hooks, service-worker, XML and JSON checks all pass.
085 v24 release preparation: Package contents include the complete v24 HTML/CSS/JS static site, v24 service-worker, bilingual recruiter `resume.html`, editable `content-releases.json`, endpoint configuration documentation, validator, light-mode audit and all local visual assets. The final archive is rebuilt after this QA entry so the attached QA evidence and archive contents remain synchronized; final size and SHA-256 are recorded in `RELEASE-MANIFEST-v24.md`.
086 v25 AI Studio runtime QA: Added grounded Ask about Rifat assistant with curated local answer and source path, optional consent-gated protected endpoint, Brief Analyzer, Fit Interpreter, Architecture Explainer, Case-study Q&A, Safe Code Review, Accessibility Rewriter, Audience Personalizer, Build-log Summarizer and Natural-language Navigator. Fresh v25.0/v25.1 client found all 12 AI/integration IDs, cache-busted script/style references and no console errors. Local outputs were verified for SQLite grounding, missing brief inputs, fit risk signals, database architecture, case-study trade-off, dynamic-execution code warning, local rewrite, recruiter personalization, v24 summary and recruiter navigation. Bengali toggle translated the AI Studio title, grounded answer, brief output and case-study answer, then restored English.
087 v25 safety and readability QA: With AI endpoint empty and all consent checkboxes unchecked, grounded assistant, brief analyzer and code reviewer made zero external AI calls and reported local modes. v25.2 fresh cache applied the readability guard: AI labels 12.8px, consent copy 13.44px, node controls 13.12px, grounding note 14.72px and AI output copy 16px in light mode. Existing 390x844 mobile CDP QA passed header menu open/close, body lock, ARIA state, five utility controls and inherited readable mobile typography. Static validator reports ids=502, images=7, forms=19, required_features=163; HTML structure, local images, image alt text, hooks and service-worker checks pass.
088 v25 packaging preparation: Final archive will include AI Studio source, `AI-FEATURES-SETUP.md`, `ai-feature-map-v24.md`, v25.1 service-worker, v25.2 cache-busted assets, updated docs, validator, mobile QA script, light-mode audit and all v24 assets. Final archive size and SHA-256 will be recorded in `RELEASE-MANIFEST-v25.md`.
089 v25.3 overflow repair QA: Desktop 1280px diagnosis found no document horizontal overflow after viewport normalization, but the fixed top navigation had a crowded flex row with brand, eight primary links, quick social links and five utility controls. Contact form descendants stayed inside their grid column, while long consent/contact copy needed explicit wrapping containment. The patch adds shrinkable header children, controlled desktop/tablet gaps, mobile navigation containment, `min-width:0` across grid/form surfaces, `overflow-wrap:anywhere` for long copy and `overflow-x:clip` for the document.
090 v25.3 responsive QA: Fresh 390px CDP audit reports `scrollWidth=390`, no header or contact descendants outside the viewport, nav height 62px, contact wrap width 358px, menu open/body lock/ARIA state pass, five mobile utility controls present, and clean close/reset. Fresh desktop 1279px audit reports `scrollWidth=1264`, header bounds left 42/right 1222 with no child outside the viewport, and no contact descendants outside the viewport. Light, dark, Bengali and restored English states all retain no header/contact overflow. Cache-busting is `style.css?v=25.3` and `script.js?v=25.3`; service-worker cache is `rifat-portfolio-v25.2`.
091 v25.4 root-cause repair: Fresh DOM audit found one actual site navigation header plus one nested assistant-panel header; the apparent header nesting was visual crowding from the quick-social row and utility controls inside the fixed nav. The quick-social row is now hidden on desktop as well as mobile so the primary navigation and display controls have a clean single-level composition. The contact QR markup is a single `#toggle-contact-qr` button controlling `#contact-qr-panel` with `contact-qr.png`; its previous behavior was obscured by the broader contact/post-contact layout rather than a missing image asset.
092 v25.4 AI/contact functional QA: The AI consent checkbox previously inherited `width:100%` from generic AI inputs, leaving the consent span at zero width and producing a tall vertical text column plus a large blank card. The repair resets checkbox dimensions to 16px, gives the consent span the remaining flex width, makes the consent row full-width and keeps the submit button on a normal row. Fresh desktop AI form metrics are now form height 174px, consent height 25px and consent span width 1082px. Local grounded assistant returned the SQLite answer after submit. Show contact QR toggled to `hidden=false`, `aria-expanded=true`, `Contact QR বন্ধ করুন`, loaded a 192x192 image, then reset cleanly.
093 v25.4 mobile QA: Fresh 390x844 CDP client reported `scrollWidth=390`, no header/contact descendants outside the viewport, AI form width 324px, consent width 324px with a 299px text span and 80px Bengali line-wrapped height, menu open/body lock/ARIA state pass, five mobile utility controls present, QR toggle opened with a 240px contained panel and reset cleanly. Static validator remains ids=502, images=7, forms=19, required_features=163; syntax and validator checks pass.
094 v25.5 screenshot-matched mobile repair: The 390px/360px audit showed the prior v25.4 card grid could still present an oversized two-column composition on a wider mobile/tablet CSS viewport. v25.5 makes the AI Studio grid a single-column flow through 1100px, keeps every card at natural height with `align-self:start`, removes output minimum-height inflation, and keeps consent/button rows inside each card. CSS/JS mobile navigation breakpoints are synchronized at 1100px so a narrow tablet/mobile header uses the compact menu and moves five utility controls into the menu.
095 v25.5 fresh mobile audit: At 390px, `scrollWidth=390`, header height 62px, zero header/card overflow children, grounded card height 544px, brief analyzer 549px, architecture explainer 338px, case-study Q&A 478px, code review 664px, and natural navigator 459px. Consent rows are horizontal and contained: grounded width 324px/height 70px, brief width 324px/height 46px, code review width 324px/height 46px. At 360px, `scrollWidth=360`, zero header/card overflow children, grounded card height 563px and natural navigator 452px; all controls remain inside 328px cards.
096 v25.5 interaction regression: Fresh 390px CDP test passed compact menu open/body scroll lock/ARIA state, five mobile utility controls, clean close/reset, QR toggle with contained 240px panel, and QR reset. Existing AI form, contact and header interactions remain available.

### 097 — v26.1 mobile surface final QA (2026-08-16)
- Fixed the AI assistant composer grid so its visually-hidden accessibility label no longer occupies a grid cell; input and Send button now stay on one row.
- Fresh preview target: `?v=26.1`.
- `cdp_surface_interaction_qa.js`: mobile viewport 390px, document scroll width 390px; Reading Progress top 66px, centered width 163px; AI panel width 367px; message font 13.44px / 22.176px; composer form 367×67px; input 266×43px; Send button 67×43px; Sections drawer 336×716px with scrollHeight 846px and last Contact item visible after scroll.
- `cdp_surface_audit.js`: AI panel 369px wide, messages remain scrollable, no overflow reported; 360px audit also retains 360px document width and top-centered progress.
- `cdp_ai_mobile_audit.js`: AI Studio cards remain single-column with natural heights and no horizontal overflow at 390px and 360px.
- `node --check script.js`, `node --check sw.js`, and `python3 validate_attachment_build.py`: all passed; validator reports 502 IDs, 7 images, 19 forms, and 163 required features.

### 098 — v27 Feature Command Center final QA (2026-08-16)
- Added a Feature Command Center with local Visitor Intent Mode, Interactive Project Evidence Room, Technical Decision Log, Deployment Health Panel, Privacy & AI Transparency, Content Freshness/Evidence Pack summary, and Accessibility Command Center links.
- Visitor intent persists locally and generates recruiter, client, developer or full-view proof paths without hiding the underlying portfolio content.
- Evidence Room switches between MRI Vault, DeepSeek Bot and Portfolio, updates architecture/decision/failure/security/next-proof fields, and exports a project-specific Markdown evidence summary.
- Deployment Health stays honest when no protected status endpoint is configured; it reports connection state rather than inventing uptime or response-time numbers.
- v27.1 focused mobile QA: 390px and 360px document widths stayed exact, seven new cards rendered without horizontal overflow, Bengali/English switching refreshed the new feature content, and light mode produced readable dark heading/card text.
- Existing regression QA: Reading Progress remained top-centered; AI assistant panel remained 367px wide with input 266px and Send 67px on one row; Sections drawer remained 716px high and scrollable to Contact; AI Studio remained single-column at 390px and 360px with zero header overflow.
- Browser console reported no output/errors. Static validator passed with 528 IDs, 7 images, 20 forms, 178 required features and service-worker cache `rifat-portfolio-v27.1`.

### 099 — v28 Professional Delivery Layer final QA (2026-08-16)
- Added Client Collaboration Charter, Contact Preflight Checker, Project Risk & Trade-off Matrix, Security Threat Model Viewer, Before/After Workflow Simulator, Claim-to-Proof Ledger, Project Handoff Preview, Design System Explorer, Release Verification and Shareable Project Routes.
- v28 focused mobile QA at 390px: 10 delivery cards rendered, one local preflight form passed its ready path, risk matrix switched to DeepSeek Bot, threat model switched to Provider, workflow switched to After, claim ledger switched to LLM integration, release verification passed, route selection generated a stable static anchor, design preview switched to blue + large, and no descendant overflow was found.
- v28 bilingual/theme QA: language switch refreshed the Professional Delivery title and dynamic risk copy in Bengali; light mode produced readable heading and card text with exact mobile width.
- Existing regression QA remained green: 390px document width stayed exact; Reading Progress remained top-centered; AI panel remained 367px wide; composer input remained 266px with a 67px Send button on one row; Sections drawer remained 716px high and scrollable to Contact; AI Studio stayed single-column at 390px and 360px with zero header overflow.
- Browser console reported no output/errors. Static validator passed with 560 IDs, 7 images, 21 forms, 206 required features and service-worker cache `rifat-portfolio-v28.0`.

### 100 — v29 Proof Lab final QA (2026-08-16)
- Added Evidence Confidence Board, Interactive Architecture Trace, Live Demo Sandbox, Performance Budget Panel, Security Disclosure, Project Maintenance Preview, Personalized Proposal Generator, Release Diff Viewer, Scroll-synchronized Proof Rail, Why This Stack rationale, and Case Study Citation Anchors.
- v29 focused mobile QA at 390px: 11 proof-lab cards rendered, architecture trace switched to Intelligence, local sandbox returned the Backup path preview without changing files or contacting Telegram, performance scan reported browser-observed resource/image/script/style counts without inventing Lighthouse/Core Web Vitals values, proposal generator produced an AI/Telegram discovery outline, release diff produced the v28 → v29 narrative, proof rail switched to Proof, and stack rationale switched to Vanilla JavaScript.
- v29 mobile document width stayed exact at 390px and no proof-lab descendant overflow was found. Light-mode heading remained readable. The test opened in Bengali from persisted state and the language toggle returned the dynamic trace content to English, validating both localized states across the run.
- Existing regression QA remained green: Reading Progress stayed top-centered; AI assistant panel remained 367px wide; composer input remained 266px with a 67px Send button on one row; Sections drawer remained 716px high and scrollable to Contact; AI Studio stayed single-column at 390px and 360px with zero header overflow.
- Browser console reported no output/errors. Static validator passed with 590 IDs, 7 images, 23 forms, 235 required features and service-worker cache `rifat-portfolio-v29.0`.

### 101 — v30 Guided Evaluation Layer final QA (2026-08-16)
- Added 60-second Guided Portfolio Tour, Interactive Technical Interview Mode, Request/Response Playground, Browser & Device Compatibility Matrix, Proof Lineage Viewer, Interactive Acceptance Criteria Builder, Release Fingerprint Panel, Assistant Conversation Export, Saved Project Shortlist, Deployment Environment Matrix, Build Decision Timeline and Screen-reader Reading Order Preview.
- v30 focused mobile QA at 390px: 12 guided-evaluation cards and two forms rendered; developer tour advanced to the request-contract step; LLM interview answer path rendered; local assistant request contract rendered without a provider call; QA lineage and responsive acceptance checklist rendered; page fingerprint calculated from the current HTML response; MRI Vault and DeepSeek Bot saved locally; Netlify deployment guidance rendered; v30 decision timeline rendered; screen-reader order preview produced six ordered landmarks.
- v30 mobile document width stayed exact at 390px and no guided-evaluation descendant overflow was found. Bengali localization and light mode were validated with readable title/card colors. Fingerprint is explicitly labeled as a page fingerprint, not a signed package attestation.
- Existing regression QA remained green: Reading Progress stayed top-centered; AI assistant panel remained 367px wide; composer input remained 266px with a 67px Send button on one row; Sections drawer remained 716px high and scrollable to Contact; AI Studio stayed single-column at 390px and 360px with zero header overflow.
- Browser console reported no output/errors. Static validator passed with 635 IDs, 7 images, 25 forms, 280 required features and service-worker cache `rifat-portfolio-v30.0`.

### 102 — v31 Evaluation Operations final QA (2026-08-16)
- Added Project Comparison Board, Self-health & Link Integrity Scanner, Real Performance Observer, SEO & Social Preview Studio, Interview Rubric Mode, Client Discovery Meeting Builder, What Changed Since Last Visit panel, Public Roadmap, Asset & Image Health Panel, Private Inquiry Draft Vault, Case Study Branching Replay, and Accessibility Scenario Simulator.
- v31 focused mobile QA at 390px: 12 operations cards rendered; MRI Vault + DeepSeek Bot comparison generated; self-health scan reported unique IDs, present local images, alt text, contained anchors, title and description; performance observer reported paint/resource/navigation availability and explicitly stated that it is not a universal score; Bengali SEO preview rendered; security interview rubric rendered; AI/LLM privacy discovery agenda rendered; roadmap filtered to the next item; inquiry draft saved to browser storage; maintenance-first case replay rendered; low-motion + large-text scenario rendered.
- v31 mobile document width stayed exact at 390px and no evaluation-operations descendant overflow was found. Language toggled back to English and light mode remained readable with title color `rgb(23, 25, 37)` and card heading color `rgb(23, 32, 51)`.
- Existing regression QA remained green: Reading Progress stayed top-centered; AI assistant panel remained 367px wide; composer input remained 266px with a 67px Send button on one row; Sections drawer remained 716px high and scrollable to Contact; AI Studio stayed single-column at 390px and 360px with zero header overflow.
- Browser console reported no output/errors. Static validator passed with 678 IDs, 7 images, 26 forms, 323 required features and service-worker cache `rifat-portfolio-v31.0`.

### 103 — v32.1 Mobile UX Layer final QA (2026-08-16)
- Added mobile Quick Start with four focused actions, an advanced-proof progressive disclosure block, mobile-safe-area CTA spacing, touch/focus refinement, AI bottom-sheet sizing, mobile typography and scroll-snap project rail behavior.
- Added progressive mobile form flow for Fit Quiz, project brief and contact forms: 7, 5 and 6 steps respectively, one current step at a time, live progress label, previous/next controls and final submit visibility only at the last step.
- Added local offline fallback status, browser-only draft-safe messaging, lazy loading/async decoding hints for non-critical images, and an assets-ready document state.
- 390px QA: Quick Start displayed at 358px wide; advanced guide displayed; CTA touch targets measured 42–48px high; form step flow advanced from Step 1 / 7 to Step 2 / 7; AI panel measured 369px wide and 467px high; sticky CTA and AI launcher no longer overlapped after v32.1; offline fallback became visible; no overflow; document width remained 390px.
- 360px QA: Quick Start displayed, document width remained exactly 360px, AI panel measured 339px wide, sticky CTA/AI launcher overlap was false, no overflow was found, and Fit Quiz, brief and contact forms exposed 7, 5 and 6 mobile steps.
- Existing regression QA remained green: Reading Progress stayed top-centered; AI composer input remained approximately 265–266px with Send button approximately 68–69px; Sections drawer remained 716px high and scrollable to Contact; AI Studio stayed single-column at 390px and 360px with zero header overflow.
- Browser console reported no output/errors. Static validator passed with 682 IDs, 7 images, 26 forms, 327 required features and service-worker cache `rifat-portfolio-v32.1`.

### 104 — v32.2 Keyboard Guide repair final QA (2026-08-16)
- Root cause: the first repair placed a `languageToggle` listener before the `const languageToggle` declaration, causing a temporal-dead-zone runtime error that stopped later initialization. The listener was moved below language initialization.
- Keyboard Guide now has an explicit `tabindex="-1"` panel, bilingual shortcut descriptions, explicit open/close labels, focus transfer into the panel, focus return to the trigger, and Escape-to-close behavior.
- Mobile 390px QA: initial panel hidden with `aria-expanded=false`; click opened the panel with `aria-expanded=true`, focus moved to `keyboard-guide-panel`, panel width was 358px, Escape closed it and returned focus to `keyboard-guide-toggle`; second click close returned focus correctly; Bengali toggle produced `Keyboard guide বন্ধ করুন` and `Command palette খুলুন`; no section overflow and document width stayed 390px.
- Desktop 1280px QA: click/open/focus/Escape/close behavior passed; panel width was 744px with no section overflow; English localization produced `Hide keyboard guide` and `Open command palette`; document width stayed 1265px.
- Existing regression QA remained green: Reading Progress top-center; AI composer input approximately 265px with a 69px Send button; Sections drawer 716px scrollable to Contact; AI Studio single-column at 390px and 360px with zero header overflow.
- Browser console reported no output/errors. Static validator passed with 682 IDs, 7 images, 26 forms, 327 required features and service-worker cache `rifat-portfolio-v32.2`.

### 105 — v32.5 Preview section repair final QA (2026-08-18)
- User-reported issue: the in-page project Preview actions were not discoverable on touch/mobile because `.project-preview` depended on hover and remained visually hidden without a hover state. The modal logic itself was also tested and was healthy.
- Repair: project-card Preview buttons now carry `project-preview--always-visible`; mobile viewport fallbacks keep them visible with touch-safe sizing. Stylesheet cache-bust is v32.5 and service-worker cache is `rifat-portfolio-v32.5`.
- 390px QA: all three project Preview buttons visible at opacity 1, 129px wide × 42px high; every button opened the project dialog with the correct title and a loaded 2560px artwork; Case Studies `Open visual case study` opened the MRI Vault dialog; zero overflow and document width stayed 390px.
- 360px QA: all three Preview buttons visible at opacity 1, 129px wide × 42px high; all three project dialogs and artwork loaded; Case Studies visual preview opened; zero overflow and document width stayed 360px.
- 1280px QA: Preview buttons remained visible at opacity 1, 122px wide × 38px high; all three dialogs and artwork loaded; Case Studies visual preview opened; zero overflow and document width stayed 1265px.
- Embedded bot preview actions and workflow reel were also exercised: Search, Retrieve, Backup and Play workflow updated their local output without external requests.
- Static validator passed with 682 IDs, 7 images, 26 forms, 327 required features and service-worker cache `rifat-portfolio-v32.5`.

### Entry 106 — v33.4 client-ready scope repair and responsive regression

The v33 client-ready operations were moved into a dedicated isolated IIFE, restoring local DOM helpers, language refresh, copy behavior, scope maps, content modes, trade-off controls, portal views, incident recovery, dependency proof, brand-kit output, quality scan, contact-card export and natural routes. The duplicate/out-of-scope helper conflict was removed and the IIFE was closed safely.

Asset references and the service-worker cache were bumped to v33.4. Mobile client-ready controls now wrap instead of creating horizontal scrolling. Static validation passed with 714 IDs, 7 images, 28 forms and 359 required features; HTML structure, local images, image alt text, hooks and service worker checks all passed.

Regression QA passed for client-ready outputs, 360px/390px/1280px overflow checks, visual case-study image loading, keyboard-guide focus/escape/close behavior, surface interactions, AI mobile composer behavior and preview opening. Visual case-study images reported naturalWidth 2560 and all tested overflow arrays were empty.

### Entry 107 — v33.5 dark/night mode contrast correction

The Client-Ready layer had a theme-specific regression: in dark mode, its section and cards used the light `--paper` background while body text inherited the same `--paper` color. Computed styles confirmed cream backgrounds and cream foregrounds, explaining the unreadable appearance shown on mobile.

A scoped `html[data-theme="dark"] .client-ready-section` contrast guard now provides dark navy section/card surfaces, white headings, blue-gray body copy, explicit dark form controls, readable output panels, gold focus/active states, and dark contact-card/portal previews. Light-mode rules were left intact.

Fresh v33.5 runtime verification confirmed dark computed colors for section/card/body/output/button/input, light-mode regression remained readable, and visual preview showed the corrected dark editorial palette. Client-ready QA passed at 360px, 390px and 1280px with zero overflow entries; preview QA passed with all tested case-study images loaded; AI mobile, surface interaction and keyboard guide regressions passed. Static validation remains 714 IDs, 7 images, 28 forms and 359 required features.

### Entry 108 — v34 reference-inspired animation upgrade

The two Dribbble references were inspected for high-level visual cues: oversized editorial typography, generous whitespace, calm hierarchy, compact navigation, portrait/device focal composition, faint background type and restrained parallax/reveal motion. No reference code, artwork, branding or exact layout was copied.

An original vanilla motion layer was added. It provides a staggered hero entrance, portrait/hero-aura depth on fine pointers, section-word drift watermarks, clip-based section/card reveals, reduced-motion and Save-Data safeguards, and a viewport fallback for long sections/content-visibility geometry. The existing magnetic controls, tilt cards, custom cursor, project previews, Bengali/English shell, dark/light theme and Client-Ready layer remain intact.

v33.7/v34 QA passed. Static validation reported 714 IDs, 7 images, 28 forms and 359 required features. Motion QA passed at 360px, 390px and 1280px with zero overflow; standard motion revealed viewport cards and reduced-motion made all cards immediately visible. Client-ready QA remained clean, preview images loaded, and AI mobile, surface interaction and keyboard guide regression checks passed.

### Entry 109 — v35 cinematic cursor experience

An original fine-pointer cursor layer was added without replacing the existing custom cursor. Desktop now receives a five-node trailing orbit, a soft halo, contextual labels such as EXPLORE/TALK/OPEN/SELECT/COPY, project/service/showcase card light fields and click bursts. The layer uses one rAF loop, transform/opacity-only positioning, and no new dependency.

The layer is gated by fine-pointer + hover support, `prefers-reduced-motion`, Save-Data and Simplified View. Mobile touch emulation resolves to coarse/no-hover and mounts no cursor layer or trails; reduced-motion desktop also mounts none. Desktop runtime confirmed five trails, EXPLORE label activation and cursor-enhanced cards. Existing Client-Ready, preview, AI mobile, surface interaction and keyboard-guide regressions passed at 360px, 390px and 1280px with zero overflow; preview images loaded. Static validation remains 714 IDs, 7 images, 28 forms and 359 required features.

### Entry 110 — v35.1 mobile floating-control containment

User-flow screenshots exposed three mobile layering problems: the open Sections drawer was visually covered by the fixed Start a conversation CTA, the AI launcher competed with floating controls, and the back-to-top arrow could occupy the same bottom lane. A mobile containment patch was added. The open drawer now receives a viewport-relative max-height with internal scrolling and safe-area padding; while `body.menu-open` is active, the sticky CTA, AI assistant, back-to-top control, section beacon and reading-progress label are hidden from hit-testing. In the normal closed state, the sticky CTA, AI launcher and back-to-top control use separate vertical lanes; the back-to-top control also hides when the CTA or AI panel is active.

Automated 390px and 360px touch-flow QA reports menu overflow 0, sticky/AI/back-to-top overlap false, AI panel fully inside the viewport, scrollWidth equal to viewport width, and menu body lock true. Existing surface interaction, AI mobile, preview, keyboard guide and Client-Ready regressions passed; preview images loaded. Static validation remains 714 IDs, 7 images, 28 forms and 359 required features.

### Entry 111 — v36 original Proof-to-Flow signature animation

A new original signature layer was added without replacing existing cursor, reveal, magnetic or accessibility behavior. On fine-pointer desktop, the layer draws a lightweight animated signal path from the live cursor to the currently hovered or keyboard-focused project, service, proof, delivery or CTA surface. The path carries a moving proof pulse, the target receives a subtle focus frame, and a contextual label identifies the current semantic route such as PROJECT / PROOF, PROOF / TRACE or DELIVERY / READY. The layer is isolated in `proof-flow-layer.js` and uses only a fixed canvas plus transform/opacity-based UI treatment.

The layer is disabled for touch/coarse pointers, mobile widths, reduced-motion preferences and Simplified View. v36 QA confirmed desktop activation with one active target and a visible Bengali contextual label, touch-mobile disablement with zero active targets, reduced-motion disablement, and no viewport overflow. Existing surface interaction, AI mobile, preview, keyboard guide, Client-Ready and static feature regression checks passed. Static validation remains 714 IDs, 7 images, 28 forms and 359 required features.

### Entry 112 — v34.3 mobile blank-content repair

User screenshots exposed a blank-content failure after the v34 Proof-to-Flow layer. Diagnosis found that essential project, service, process and Client-Ready cards could remain under `html.motion-ready .motion-card { opacity: 0 }` when IntersectionObserver, hash navigation, content-visibility recalculation, cached navigation, or an isolated script timing edge case prevented `is-motion-visible` from being applied. The first failsafe attempt also had an ordering weakness because it could collect targets before the motion initializer added its classes.

The repair adds `motion-failsafe.js` after the main motion initializer with dynamic target collection, MutationObserver support, pageshow/scroll/resize recovery, and hidden-content recovery. A mobile CSS safety rule additionally guarantees that essential `.motion-card`, `.motion-rise` and `.reveal-item` content is visible at widths up to 760px even if an observer or service-worker state is stale. Asset and service-worker cache versions were bumped to v34.3. All JavaScript/service-worker syntax checks and the static validator passed; the validator remains 714 IDs, 7 images, 28 forms and 359 required features.

### Entry 113 — v34.4 preview loading reliability repair

The preview blank/loading complaint was investigated against the fresh preview URL. The public HTML rendered successfully through the page extractor, and live HTTP checks returned status 200 for the document, style.css, script.js, proof-flow-layer.js, motion-failsafe.js and sw.js. The repair hardens the service worker: CACHE_NAME is now v34.4, the two motion scripts are included in the core asset list, installation uses `Promise.allSettled` so one optional/missing asset cannot abort installation, navigation requests use a 4.5-second network timeout with cached index fallback, and code/style assets use cache-first with a bounded network fallback. Old caches are deleted during activation and the latest cache takes control immediately.

Index references were bumped to v34.4. JavaScript, service-worker syntax and static validator checks passed; required feature counts remain 714 IDs, 7 images, 28 forms and 359 required features. The fresh preview document and critical assets were all reachable with HTTP 200 responses.
